import React from "react";
import Modal from "../UI/Modal";

const AddMenu = (props) => {
  return (
    <Modal onClose={props.onClose}>
      <div></div>
      <div></div>
      <div></div>
    </Modal>
  );
};

export default AddMenu;
